# 京王線 連続立体交差事業(笹塚〜仙川)3Dビューア — Claude Code 引継書

このプロジェクトを Claude Code で継続開発するための作業ルールと現状。
**作業を始める前に必ずこのファイルと `TEMPLATE.md` を読むこと。**

---

## プロジェクト構成

```
keio-3d-viewer/
├── keio_elevated_3d.html    ← 唯一の成果物(単一HTML+Three.js r128)
├── CLAUDE.md                ← このファイル(作業ルール・現状)
├── TEMPLATE.md              ← 制作テンプレート(設計思想・躓き集・鉄則)
├── reference/               ← 実車参考写真の置き場(※現在 空。経緯はREADME参照)
└── tools/                   ← 検証ツール一式
    ├── verify_all.sh        ← 全チェック一括実行
    ├── check_syntax.py      ← ①JS構文チェック(node --check)
    ├── harness.js           ← ②スタブ実行(ReferenceError・初期化順序バグ検出)
    ├── auto_check.py        ← ③諸元表 ⇄ 実車実測値(ソースレベル)
    ├── verify_car.js        ← ④実ジオメトリ ⇄ 実車実測値(実行して頂点を測定)
    ├── verify_run.js        ← ⑤自動運転(停止位置・停車時間・走行線・ホームドア)
    ├── verify_line.js       ← ⑥高架橋の桁幅・架線
    ├── stub_three.js        ← 検証用の共通スタブ(THREEを測定可能な実体に差し替える)
    └── patch_helper.py      ← 厳密一致置換のヘルパー
```

**実車写真について**:元の `keio8000_front.png` / `keio8000_side.png` は失われている。
ただし写真の画素解析で得た**実測値**は失われておらず、HTML側は諸元表
(`K8`/`BANDS`/`WIN`/`FWIN`)に、検証側は `auto_check.py` と `verify_car.js` の
`REF` に、それぞれ独立して保持している。
**検証側の REF を HTML から読ませてはいけない**(モデルを直せば基準も動く循環になる)。

---

## 作業ルール(厳守)

### 1. チェックポイント方式
コードを1回編集するたびに、以下を通してから次へ進む。**どれか失敗したら先に進まない。**

```bash
bash tools/verify_all.sh
```

内訳:①構文 → ②スタブ実行 → ③諸元表 → ④実ジオメトリ → ⑤自動運転 → ⑥桁幅・架線。
これにより、いつ中断しても `keio_elevated_3d.html` は常に整合状態に保たれる。

### 2. 編集は厳密一致置換で
直接 str_replace か、`tools/patch_helper.py` の `Patcher` を使う。
**置換対象が想定回数だけ出現するか検査し、違えば書き込む前に停止する**こと。
一度に大量の変更をせず、1回の作業は数項目まで。

### 3. 「見た目確認」は自分でできない前提で
Claude(AI)は自分が生成した3D描画を安定して見られない。見た目の検証は:
- **数値で**(桁幅マージン、加減速特性、停止位置、線形の曲線半径 等をスクリプトで)
- **実測値との照合で**(`auto_check.py`=表を検査 / `verify_car.js`=出来たジオメトリを実測)
- **人間のスクリーンショットで**(最終確認は人に依頼)

「確認しました」で済ませず、必ず上記のいずれかを実行する。

### 4. 座標系(絶対に崩さない)
- `s` = 起点(笹塚)中心からの営業距離[m]、`off` = 線路直角方向[m](+=北)
- ワールド座標は地理準拠(X=東, Z=南, Y=高さ)。真上から見て北が上・新宿方が右
- 全構造物は `frame(s, off)` 経由で配置。線形(RAW配列)を変えれば全体が追従

---

## 現状(実装済み)

- 実測WGS84座標由来の線形エンジン(全駅・曲線区間・フィレット接続)
- 全駅のホーム(210m)・配線・副本線・留置線・両渡り分岐器
- 高架橋(桁幅は最外軌道+2.45mを下限に防御的に決定)・橋脚
- 架線・架線柱(`poleXs()`で位置を一元化、架線の吊り点=柱の真下)
- 京王8000系(**2026-07 全面作り直し。写真転写方式は廃止**)
  - 諸元表 `K8`/`BANDS`/`WIN`/`FWIN` → ジオメトリ生成 の一方向データフロー。
    表の数値を書き換えれば車体・帯・窓・前面がすべて追従する
  - 車体はロフト(断面スイープ+頂点カラーの帯)、窓・扉は車体表面の外側に置く
    独立パネル(ロフトの分割数と無関係に輪郭が鋭く出る)
  - 前面もジオメトリ:3分割の曲面ガラス・貫通扉・行先表示器・灯具・スカート
  - 窓割りは扉配置 `K8.DOORX` から自動生成(中間車 1-2-2-2-1、先頭車は
    車端側を乗務員扉+乗務員室側窓に置換)
  - 足回り:台車 TS-823A/TS-824 相当(側梁・空気ばね・軸箱・ブレーキディスク)、
    床下機器は VVVF/SIV/CP/蓄電池/空気タンクに分割
  - 屋根上:**集中式冷房1基**+前後のダクトカバー(旧実装は3基載せており誤り)、
    パンタは菱形 PT-4201 相当
  - LODで近景=高精細/遠景=簡素を自動切替。遠景では灯具・幌・配管も省く
  - 三角形数は旧実装の約1/3(先頭車 98,136 → 31,902)
- 軌間1372mm(馬車軌間)を `GAUGE` で一元化。レールの敷設も車輪位置も同じ定数から導く
- 停止位置はホームの実範囲 `platRange()` から求め、進行方向ごとに反対端へ着ける。
  目標は先頭車の"中心"(t.x の意味と一致させる)。駅別補正は `STOP_ADJ`
- ホームドア(可動式ホーム柵):1両4箇所×10両=40箇所×20列。開口位置は停止位置と
  `K8.DOORX` から算出するので、停止位置や扉配置を変えれば開口も追従する。
  停車中(発車3秒前まで)だけ開く
- 八幡山:停車列車はホーム横の停車線(内側±4.55m)を走る(`runOff()`)。通過線は外側±8.35m。
  停車時間は駅別表 `DWELL_OF` で八幡山のみ60秒
- 桁半幅は表を持たず「最外軌道+2.45m / ホーム外縁+0.6m / 標準4.45m」の最大で決まる
- 架線柱の位置は `POLE_S` に一元化。軌道と架線は `layTrack()` で必ず対にして作り、
  登録簿 `TRACKS`/`WIRES` に記録する(線路があるのに架線が無い、が起こらない)
- 自動運転(3本):各駅停車(加速P4/上限60km/h/減速B5/停車25秒)、状態機械
- カメラ:俯瞰/列車追尾/運転モード(ワンハンドルマスコン・次駅表示・首振り)
- スマホ対応(横画面前提・UIスライド開閉・運転中ラベル強制OFF・負荷軽減)

## 検証の基準値(写真実測。レール面基準の高さ[m])

| 項目 | 実測値 | 保持場所 |
|---|---|---|
| 車体幅 / 床面 / 肩 / 屋根 | 2.845 / 0.95 / 3.15 / 3.64 | `K8` |
| 京王レッド帯 | 1.43〜1.66 | `BANDS` |
| 京王ブルー細線(赤帯の**上**) | 1.83〜1.87 | `BANDS` |
| 側窓 | 2.27〜3.05 | `WIN` |
| 前面窓 | 1.95〜3.42 | `FWIN` |

`auto_check.py`(表を読む)と `verify_car.js`(生成されたジオメトリの頂点を測る)の
両方で照合する。前者だけだと「表は正しいがコードが表を無視している」状態を、
後者だけだと「モデルに合わせて表を書き換えた」状態を見逃す。

`verify_car.js` は寸法の照合に加えて**幾何的な整合**も見る:側面トリムが車体表面に
密着しているか、前面部品が前面輪郭からはみ出していないか、機器が車体幅の内側か・
レール面より上か、客用窓と扉が干渉しないか、編成内で車両同士が干渉しないか。
(実際にこの検査で行先表示器が屋根を突き抜けている不具合を検出・修正した)

**公表諸元由来**(写真ではなく資料):台車 TS-823A(電動)/TS-824(付随)=東急車輛製・
車体直結式空気ばね、冷房=屋根上集中式 48.84kW を1両1基、パンタ=東洋 PT-4201(菱形)、
前頭部〜乗務員扉は普通鋼のアイボリー塗装(側面は軽量ステンレス)。
台車中心間距離13.60m・固定軸距2.20m・車輪径860mmは20m級の標準値で**出典未確認**。

---

## よくある落とし穴(TEMPLATE.md §6 に詳細)

- 配列の行末コメントが次要素を飲み込む → 配列は1行1要素
- 後面テクスチャが消える → ポリゴン巻き順+material の DoubleSide
- 前面テクスチャが小さい → 「前面幅=車体幅」で対応付け(推測係数を挟まない)
- 列車が端で折り重なる → 範囲外は非表示+端で初期位置リセット
- カーブ/勾配で車体が傾かない → 車端2点の実位置の弦で姿勢決定
- harnessでブラウザAPI未定義 → tools/harness.js にスタブを足す
- 断面リングの頂点数が場所で変わり接続が壊れる → 帯の境界は固定し、肩から上の
  埋め点だけを**個数固定**で等分する(旧実装は先頭部でリング長が変化していた)
- 検証の基準値をHTMLから読んで循環する → 基準は検証スクリプト側に持つ
- 停止位置を駅中心から一律に測る → ホームがずれている駅(笹塚)で狂う。実範囲から測る
- 列車の t.x は先頭車の"中心"。停止位置を先頭位置として与えると車体半長ぶんずれる
- 進行方向で反転すべき補正を反転し忘れる → 上り列車だけホームからはみ出す
- 架線の吊り点を場所ごとに再計算する → 柱の真下から外れる。柱位置は一元化する
- 開閉アニメの更新条件に不感帯を作る → 半開きのまま残る(端で値を止め切る)
- 窓を頂点カラーで塗る → 独立パネルにする。分割数に輪郭が左右されない

---

## 次にできること(未着手・改善余地)

- 側面も写真テクスチャ方式へ(現状は頂点カラー)
- 駅ごとの発車メロディ・走行音(Web Audio)
- 時刻表に基づく運行ダイヤ(現在は等間隔の自動運転)
- 縦断図の勾配数値の精緻化(公式縦断図があれば)
- 他線区への横展開(TEMPLATE.md の手順に従う)

---

## ローカルでの開き方

`keio_elevated_3d.html` をブラウザで開くだけ(Three.js を cdnjs から読むので
初回はネット接続が必要)。スマホは横画面。
検証ツールの依存:Node.js、Python3 + numpy + pillow。
