# 鉄道連続立体交差事業 3Dビューア制作テンプレート(指示書・引継書)

京王線(笹塚〜仙川)高架化3Dビューア制作の全工程から得た知見をまとめた、
**別路線で同種のものをAIと作るための手順書**。
「AIが何をできないか」を前提に組み立ててあるので、同じ轍を踏まずに済む。

---

## 0. 大原則(最初に共有すべきAIの制約)

| AIの制約 | 対策 |
|---|---|
| **地図・航空写真は見られない** | 実測座標(WGS84)を人間が用意して渡す。「◯◯駅の位置」とだけ言っても正確な線形は作れない |
| **自分が生成した描画結果を安定して"見る"ことができない** | ①人間がスクリーンショットを撮って渡す(AIはアップロード画像なら読める) ②写真を画素解析→数値照合する自動検証スクリプトを作らせる。「見た目を確認して」という指示は機能しない |
| **「似せて」だけでは実物に近づかない** | 実車写真を渡し、**写真そのものをテクスチャ化**させる(後述の車両モデリングの鉄則) |
| **長いセッションで文脈を失う・モデルが切り替わる** | チェックポイント方式(後述)で、いつ中断・再開しても壊れない作業手順を最初に約束させる |
| **一度に大量の変更をさせると壊す** | 1回の指示は3〜6項目まで。各項目は「何を・どこを・どう変えるか」を具体的に |

---

## 1. 人間が事前に用意するもの(データパック)

### 1-1. 必須:実測座標(これが無いと線形が作れない)
Googleマップ等で自分で測って渡す。**各駅のホーム両端2点**のWGS84座標(緯度・経度)。

```
駅名, 種別(島式1面2線 等), 東端lat, 東端lon, 西端lat, 西端lon
例: 明大前, 島式2面4線, 35.6688, 139.6367, 35.6684, 139.6345
```

- 曲線区間は「どの駅間のどちら寄りが曲がっているか」+可能なら曲線上の中間点座標も数点
- 事業起点・終点の座標
- 交差する道路・鉄道・河川の交点座標(または「◯◯駅から東へ約300m」等の相対表現)

### 1-2. 公式資料(PDF・画像で渡す)
- **配線略図**(駅ごとの番線構成・留置線・渡り線)…最重要
- 駅舎デザインのイメージパース(側面意匠の作り分けに使う)
- 縦断図(高架の高さ・勾配)があれば
- 踏切の名称一覧(除却対象の実名リスト)
- 施工順序図(工事手順モードを作る場合。今回は最終的に削除した)

### 1-3. 車両の実車写真(似せたいなら必須)
- **正面**をなるべく真正面から1枚(前面テクスチャの原板になる)
- **側面**が大きく写った1枚(帯・窓の高さの実測に使う)
- 車体幅・全長などの公称寸法(Wikipediaで可。AIに検索させてもよい)

---

## 2. 最初にAIへ渡す指示文(コピペ用テンプレ)

```
◯◯線の連続立体交差事業(△△〜□□間)の高架化完成後を、単一HTML+Three.js
(cdnjsからr128読込)のインタラクティブ3Dで作ってください。

【作業方式(厳守)】
- 修正は必ずPythonパッチスクリプト方式:厳密一致置換(不一致なら停止)→
  node --check で構文検査 → スタブ実行(THREE/document/ImageをProxy化した
  harness.js)で ReferenceError 検出、まで通して1チェックポイントとする。
- どの時点で中断してもファイルが整合状態にあること。ヘレドキュメントが長く
  なる場合は create_file でスクリプトをファイル化してから実行すること。

【座標系(厳守)】
- s = 起点駅中心からの営業距離[m]、off = 線路直角方向[m](+=北)。
- ワールド座標は地理準拠(X=東, Z=南, Y=高さ)。真上から見て北が上・
  起点方が右=公式平面図と同じ向きにする。
- すべての構造物は frame(s, off) 関数経由で配置する(線形を変えたら全て追従)。

【線形エンジン】
- 添付の実測座標から生成。各駅の両端2点を通す直線+指定曲線区間を円弧、
  その他はフィレット接続。方位角は±πの跨ぎをアンラップし平滑化。
- 座標データ(RAW)を編集すれば全体が自動追従する構造にすること。

【防御的設計(厳守)】
- 高架橋の桁半幅は「その地点の最外軌道オフセット+2.45m」を常に下限とし、
  軌道や車両が桁からはみ出さないことを構造的に保証する。
- 架線柱は最外軌道+約2m、スパンは直線50m/曲線25m。架線の吊り点(ちょう架線の
  垂れが0になる点・ハンガー)は架線柱の真下に一致させる。
- 列車は「前後の車端2点の実位置を結ぶ弦」で配置する(曲線・勾配に必ず追従)。
- 事業範囲外に出た車両は非表示にし、端に達したら初期位置へ即時リセット
  (端で線形が丸められて車両が折り重なる事故を防ぐ)。
```

---

## 3. 制作フェーズの推奨順序

1. **線形エンジン+高架橋+本線2線**(まず1本の"背骨"を通す)
2. 駅(ホーム・配線・副本線・留置線・渡り線)…配線略図に忠実に
3. 架線・架線柱(柱位置を関数化し、架線の吊り点と共有する)
4. 周辺(道路・交差鉄道・街並み・樹木・ランドマーク)
5. 列車(モデリングは後回しにし、まず箱でよいので走らせる)
6. カメラモード(俯瞰/列車追尾/運転モード)
7. 車両の本格モデリング(→ §4 の鉄則に従う)
8. UI整理・スマホ対応・軽量化

**先に動く全体を作り、車両の見た目は最後**。車両は最も反復が必要な工程なので、
検証体制(§5)を整えてから着手する。

---

## 4. 車両モデリングの鉄則(今回最も苦労した領域)

### 失敗した順に:やってはいけないこと
1. **「テクスチャを貼った直方体」**…レゴのようになる。却下される
2. **条件式(if文)で窓・帯を頂点カラー塗り**…帯の上下関係や寸法をAIが誤認
   したまま何度も作り直す羽目になる(今回、紺帯を赤帯の下に置く誤りを長期間
   気づけなかった)
3. **ポリゴン数だけ増やす**…「滑らかになっただけで似ていない」と言われる。
   色境界が頂点位置と一致していなければ滲みは消えない

### 成功した最終形:写真=モデルを構造的に保証する
1. **寸法は写真の画素解析で実測する**
   - 正面写真を縦スキャンして色の遷移(窓/帯/ライト)の行番号を取得
   - **スケール基準は「正面シルエットの幅=車体幅(公称値)」**。
     ※「前面は車体幅の84%」等の推測係数を挟むと縮小して貼られる(失敗済み)
2. **前面は写真そのものを1ドット単位で実寸展開してテクスチャ化**
   - 前頭部の輪郭(行ごとの左右端)を検出し、車体座標(z,y)へバイリニア再標本化
   - PNG(64色量子化・数十KB)にしてHTMLへbase64埋込
   - UVは `u=(z+W/2)/W, v=(y-床)/(屋根-床)` の単純写像(歪まない)
   - 明度はシーン照明に合わせ 1.2倍程度に補正
3. **車体はロフト(断面スイープ)+頂点カラー**
   - 丸屋根断面を100分割級でスイープ。**帯・窓・扉の境界に頂点を正確に置き、
     境界前後で頂点を二重化**(EPS≒4mm)して色滲みを防ぐ
   - 近景=高精細/遠景=簡素のLOD切替で負荷を抑える
4. **後面(最後尾)も同じテクスチャ面を配置**。ポリゴンの巻き順を前後で
   共通にしないと裏面カリングで消える(失敗済み)

### 検証(人を介さない反復チェック)
- `verify_face.py`:HTML内テクスチャをデコード→写真から独立に測った基準値
  (窓上下端・帯上下端)と±0.10mで照合。旧デザインの残存もgrepで検査
- `auto_check.py`:側面の帯・窓のモデル定数を写真実測と照合
- これらが**PASSするまでAIだけで反復**させ、PASS後に人間がスクショで最終確認

---

## 5. 検証体制(最初に構築させる)

```
チェックポイント = ①パッチ適用(厳密一致置換、失敗時は書込前に停止)
                → ②node --check(構文)
                → ③harness.js(スタブ実行。THREE/document/Image をProxy化し
                   ReferenceError・初期化順序バグを検出)
                → ④必要に応じ数値検証(線形の最小曲線半径、桁幅マージン、
                   加減速特性、停止位置、自動運転シミュレーション等)
```

- 数値検証の例:「全線で 桁半幅−最外軌道 ≥ 2.34m を10m刻みでスキャン」
  「自動運転で1駅間を走らせ、上限速度厳守と停止位置正着を確認」
- **アサートは必ず書き込みの"前"に置く**(今回、書込後assertで中途半端な
  状態のファイルができた事故あり)

---

## 6. 躓きポイント集(実際に起きた事故と対策)

| 事故 | 原因 | 対策 |
|---|---|---|
| 配列の後半要素が消えて桁幅が異常に狭い | **行末コメントが配列要素を飲み込んだ** | 配列は1行1要素+コメントは行頭または独立行 |
| ヘレドキュメントのパッチが途中で切れて実行失敗 | スクリプトが長すぎて転送途中で切断 | 長いパッチは create_file でファイル化してから実行 |
| harnessで `Image is not defined` | ブラウザ専用APIのスタブ漏れ | harnessに Image/Canvas 等のスタブを追加していく運用 |
| `zDown is not defined`(前方参照) | セクション順序を跨いだ参照 | スタブ実行が検出してくれる。定義済みの関数(mainOff等)で書き直す |
| 後面テクスチャが表示されない | ポリゴン巻き順の反転で裏面カリング | 巻き順は前後共通+material側 DoubleSide |
| 前面テクスチャが小さく貼られる | 「前面幅=車体幅の84%」という推測係数 | シルエット幅=車体幅として対応付ける(推測係数を挟まない) |
| ノーズが黒い塊になる | 「高さ条件だけ」で黒を塗り、断面全周が黒に | 塗り分けは前面(先端からの距離+断面内位置)で限定。最終的に写真テクスチャ化で根治 |
| 列車が画面端で折り重なる | 線形関数が範囲外の距離を端に丸める | 範囲外の車両は非表示+端到達で初期位置へ即時リセット |
| カーブ・勾配で車体が線路を向かない | 方位角の取得経路のバグ | **車端2点の実位置の弦**で姿勢を決める(原因調査より確実) |
| 運転モードの画面中央が線路正面を向かない | 注視点を遠方(150m先)に置いた/オフセット違いの2点で方向を計算 | 同一オフセット上の前後±6mから接線(勾配込み)を算出して注視 |
| 架線の吊り位置と架線柱がずれる | 架線と柱で刻み・基準が別々 | 柱位置を`poleXs()`に一元化し、架線の垂れ・ハンガーも同じ配列を参照 |
| ラベルの文字がはみ出す | 固定フォントサイズ | measureTextで自動縮小 |
| モデル切替(コンパクション)で文脈消失 | — | チェックポイント方式+「適用済みマーカーをgrepで確認してから未適用分のみ実行」を徹底 |

---

## 7. 機能チェックリスト(今回の最終仕様)

- [ ] 俯瞰ビュー(初期視点=地図と同じ向き)/駅クリックでズーム
- [ ] 操作系:ドラッグ=移動、Shift・Ctrl+ドラッグ=回転、ホイール/ピンチ=ズーム
      (※移動のdy符号は要確認。今回は上下逆で1回修正)
- [ ] 列車の自動運転:各駅停車(加速P4/上限60km/h/減速B5/停車25秒)を
      状態機械('run'→'brake'→'dwell')で実装。本数は3本程度で十分
- [ ] 列車追尾モード(注視点=列車、ドラッグで角度・ホイールで距離)
- [ ] 運転モード:ワンハンドルマスコン(力行4段/中立/制動7段/非常EB)、
      ↓=力行・↑=制動・N・E キー+画面ボタン、段位チップ表示(P4〜EB)、
      速度計(120km/h以上赤字)、次駅と停止位置まで距離、
      加減速の速度特性(力行:120km/hで25%、制動:120km/hで75%→60km/h以下100%)、
      応答の一次遅れ(0.75秒)、ドラッグで首振り・ダブルタップで正面復帰
- [ ] 停止位置=ホーム先端の3m手前(起点駅など特例は個別指定)
- [ ] ラベル類:駅名/踏切名(道路名も踏切トグルに連動)、トグルボタン
- [ ] 縦断図ナビ(クリックでジャンプ)…画面上部、開閉タブ付き
- [ ] スマホ対応:横画面前提(縦は回転促しオーバーレイ)、UI縮小、
      パネルのスライド開閉、運転モード中はラベル系ボタンを強制OFF+非活性、
      pixelRatio上限1.5・LOD距離短縮で負荷減

---

## 8. 進め方のコツ(人間側)

- **スクリーンショットを惜しまない**。AIのモデリング反復は「スクショ→差分指摘」
  が最速のループ。特に車両は必須
- 「似てない」だけでなく**どこが違うか1点でも言語化**すると収束が数倍速い
  (例:「紺線は赤帯の上」「前面窓はもっと大きい」)
- AIが「確認しました」と言っても**画像比較を実際に行ったか疑う**。
  「文章ではなく画像/数値で比較して」と明示的に要求する
- 大改修の前に「現状の実装を確認して」と言わせる(anchor不一致の置換事故を防ぐ)
- 定期的に「コード整理・コメント付与・軽量化」の回を挟む

---

*このテンプレートは、京王線 連続立体交差事業(笹塚〜仙川)3Dビューア制作
セッション(実測座標線形/京王8000系写真準拠モデリング/自動検証体制)の
実績に基づく。*
