# 鉄道 3Dビューア制作テンプレート(指示書・引継書)

京王線(笹塚〜仙川)連続立体交差事業 3Dビューア制作の全工程から得た知見をまとめた、
**別路線で同種のものをAIと作るための手順書**。
「AIが何をできないか」を前提に組み立ててあるので、同じ轍を踏まずに済む。

> **改訂 2026-07**:車両の作り方を全面的に書き換えた(§4)。
> 旧版は「実車の正面写真を1ドット単位でテクスチャ化する」方式を推奨していたが、
> この方式は**廃止**した。理由と代わりの方式は §4 に書いた。
> あわせて検証体制(§5)・機能一覧(§7)を現状に合わせ、横展開の手順(§9)を追加した。

---

## 0. 大原則(最初に共有すべきAIの制約)

| AIの制約 | 対策 |
|---|---|
| **地図・航空写真は見られない** | 実測座標(WGS84)を人間が用意して渡す。「◯◯駅の位置」とだけ言っても正確な線形は作れない |
| **自分が生成した描画結果を安定して"見る"ことができない** | ①人間がスクリーンショットを撮って渡す(AIはアップロード画像なら読める) ②**数値で照合する自動検証スクリプト**を作らせる。「見た目を確認して」という指示は機能しない |
| **「似せて」だけでは実物に近づかない** | 写真を渡すより、**寸法を言語化した仕様書(mm)**を渡すほうが速く正確に収束する(§4) |
| **長いセッションで文脈を失う・モデルが切り替わる** | チェックポイント方式(§5)で、いつ中断・再開しても壊れない作業手順を最初に約束させる |
| **一度に大量の変更をさせると壊す** | 1回の指示は3〜6項目まで。各項目は「何を・どこを・どう変えるか」を具体的に |
| **自分が書いた検査を無意識に甘くする** | 検証の基準値は**モデルから読ませない**。さらに**故障注入**(わざと壊して検査がNGを出すか見る)を毎回やらせる(§5) |

---

## 1. 人間が事前に用意するもの(データパック)

### 1-1. 必須:実測座標(これが無いと線形が作れない)
Googleマップ等で自分で測って渡す。**各駅のホーム両端2点**のWGS84座標(緯度・経度)。

```
駅名, 種別(島式1面2線 等), 東端lat, 東端lon, 西端lat, 西端lon
例: 明大前, 島式2面4線, 35.6688, 139.6367, 35.6684, 139.6345
```

- 曲線区間は「どの駅間のどちら寄りが曲がっているか」+可能なら曲線上の中間点座標も数点
- 事業起点・終点の座標
- 交差する道路・鉄道・河川の交点座標
  - **交差鉄道は「交点」だけでなく「線路の方向」も必要**。交点+軸上の2点を渡す。
    s一定の帯では覆えない(§6)

### 1-2. 公式資料(PDF・画像で渡す)
- **配線略図**(駅ごとの番線構成・留置線・渡り線)…最重要
- 駅舎デザインのイメージパース(側面意匠の作り分けに使う)
- 縦断図(高架の高さ・勾配)があれば
- 踏切の名称一覧(除却対象の実名リスト)

### 1-3. 車両の寸法(**写真より仕様書**)

旧版は「実車写真を渡してテクスチャ化させる」ことを勧めていたが、**推奨しない**。
代わりに以下を用意する。

- **前面のプロポーション仕様書**(単位mm・車体底面基準)。これが最も効いた。
  実例として本プロジェクトで使った項目:
  ```
  座標系   : X=左右(中心0)、Y=高さ(車体底面0)、全幅2800 / 全高2800 の相対寸法で書く
  帯       : 青 Y=900〜950 / 赤 Y=950〜1100(上下関係と接し方を明記)
  前面窓   : Y=1200〜2500、X=±1350、角丸R150
  貫通扉   : X=±400、枠10mm
  表示器   : 種別 400×150 / 行先 600×150、Y=2350
  灯具     : ケース300×120(R20)、ランプØ80、中心間120、内=前照灯/外=尾灯
  排障器   : Y=0〜-500、中央±400は-200まで切り欠き
  ノーズ   : Y=0で奥行150 / Y=900で0(最前) / Y=2800で400、側面へ+300
  ```
- 側面の帯・窓の高さ(レール面基準[m])。写真の画素解析でもよいが、**出典を必ず記録**する
- 公称寸法(車体長・幅・台車中心間距離・軸距・車輪径)

> **寸法の出典は必ず記号を付けて分類する**。本プロジェクトでは
> [B]=写真実測 / [C]=形式標準値(出典未確認) / [D]=利用者提供の仕様書 とした。
> 混ざると「どれを信じて直すか」が決まらなくなる。

---

## 2. 最初にAIへ渡す指示文(コピペ用テンプレ)

```
◯◯線(△△〜□□間)の完成後を、単一HTML+Three.js(cdnjsからr128読込)の
インタラクティブ3Dで作ってください。

【作業方式(厳守)】
- 修正は必ず厳密一致置換(不一致なら書き込む前に停止)。
- 1回の編集ごとに検証を全部通してから次へ進む。どれか失敗したら先に進まない。
- どの時点で中断してもファイルが整合状態にあること。

【座標系(厳守)】
- s = 起点駅中心からの営業距離[m]、off = 線路直角方向[m](+=北)。
- ワールド座標は地理準拠(X=東, Z=南, Y=高さ)。真上から見て北が上・
  起点方が右=公式平面図と同じ向きにする。
- すべての構造物は frame(s, off) 関数経由で配置する(線形を変えたら全て追従)。
- 車体ローカル+zがワールドのどちら側を向くかは、線形の向きと進行方向から
  「計算」すること。推論で決め打たないこと。

【線形エンジン】
- 添付の実測座標から生成。各駅の両端2点を通す直線+指定曲線区間を円弧、
  その他はフィレット接続。方位角は±πの跨ぎをアンラップし平滑化。
- 座標データ(RAW)を編集すれば全体が自動追従する構造にすること。

【データフロー(厳守)】
- 車両は「諸元表 → ジオメトリ生成」の一方向。表の数値を書き換えれば
  車体・帯・窓・前面がすべて追従すること。逆向き(モデルに合わせて表を直す)は禁止。
- 同じ量を2箇所に書かない。軌間・架線柱位置・桁半幅・停止位置は
  すべて一元化した定数/関数から導くこと。

【防御的設計(厳守)】
- 高架橋の桁半幅は「その地点の最外軌道オフセット+2.45m」を常に下限とし、
  軌道や車両が桁からはみ出さないことを構造的に保証する。
- 軌道と架線は必ず対にして作る(線路があるのに架線が無い、が起こらないように)。
- 列車は「前後の車端2点の実位置を結ぶ弦」で配置する(曲線・勾配に必ず追従)。
- 事業範囲外に出た車両は非表示にし、端に達したら初期位置へ即時リセット。

【検証(最初に作る)】
- 数値検証スクリプト一式を先に作る(§5の7チェックポイント)。
- 検証の基準値は検証スクリプト側に持ち、HTMLから読まないこと。
- 変更のたびに「わざと壊して検査がNGを出すか」も確認して報告すること。
```

---

## 3. 制作フェーズの推奨順序

1. **線形エンジン+高架橋+本線2線**(まず1本の"背骨"を通す)
2. 駅(ホーム・配線・副本線・留置線・渡り線)…配線略図に忠実に
3. 架線・架線柱(柱位置を関数化し、架線の吊り点と共有する)
4. 周辺(道路・交差鉄道・街並み・樹木・ランドマーク)
5. 列車(モデリングは後回しにし、まず箱でよいので走らせる)
6. カメラモード(俯瞰/列車追尾/運転モード)
7. 車両の本格モデリング(→ §4 の鉄則に従う)
8. ホームドア・客用扉・保安装置(→ 停止位置と扉配置が固まってから)
9. UI整理・スマホ対応・軽量化

**先に動く全体を作り、車両の見た目は最後**。車両は最も反復が必要な工程なので、
検証体制(§5)を整えてから着手する。
**ホームドアは車両の扉配置が確定してから**着手する(開口位置を扉配置から
自動生成するので、先に作ると作り直しになる)。

---

## 4. 車両モデリングの鉄則

### 4-1. やってはいけないこと(失敗した順)

1. **「テクスチャを貼った直方体」**…レゴのようになる。却下される
2. **条件式(if文)で窓・帯を頂点カラー塗り**…帯の上下関係や寸法をAIが誤認
   したまま何度も作り直す羽目になる(紺帯と赤帯の上下を長期間取り違えた)
3. **ポリゴン数だけ増やす**…「滑らかになっただけで似ていない」と言われる
4. **写真を1ドット単位でテクスチャ化する(旧推奨。廃止)**
   - 一見よさそうだが、**指摘に対して直せない**。「帯を3cm下げて」と言われても
     テクスチャは画素の塊なので、原板を作り直すしかない
   - 元写真を失うと**二度と直せなくなる**(実際に起きた)
   - 前面だけテクスチャ、側面は頂点カラー、という不整合が残る
   - → **全面的にジオメトリへ作り直した**。以下がその方式

### 4-2. 成功した方式:諸元表 → ジオメトリ の一方向データフロー

```
K8    : 車体長・幅・床面・肩・屋根・台車中心間・軸距・車輪径・扉配置 DOORX
BANDS : 高さ方向の帯(腰板/青帯/隙間/赤帯/幕板/屋根)を下から順に並べた表
WIN   : 側窓の上下端        FWIN : 前面窓の上下端
[D]の mm 値 : 前面のすべて(帯・ガラス・貫通扉・表示器・灯具・排障器・ノーズ)
                       ↓  生成器はこの表しか読まない
車体ロフト / 窓パネル / 扉 / 前面 / 台車 / 屋根上 / パンタ
```

- **表の数値を書き換えれば全部が追従する**。逆は禁止(モデルに合わせて表を直すと、
  検証が「表とモデルが一致」を見ているだけの空検査になる)
- 仕様書[D]は相対寸法(mm)なので、実寸への縮尺 `SPX`/`SPY` を1箇所で持ち、
  前面の寸法は必ず `fz()`(左右)/`fy()`(高さ)/`fh()`(高さ方向の大きさ)/
  `fd()`(奥行き)を経由させる。**推測係数を挟まない**
- 実地確認の調整は**仕様書の表を書き換えず、係数で持つ**
  (例:`RED_THICK=1.44` / `SIDE_RED_UP=0.10`)。仕様書へ戻したくなったら係数を1に戻す

### 4-3. 形状の作り方

- **車体はロフト(断面スイープ)+頂点カラーの帯**
  - 断面リングは**必ず巡回させる**(閉じないと床が張られず下から中が透ける)
  - 帯の境界は固定し、**肩から上の埋め点だけを"個数固定"で等分**する。
    位置によってリング頂点数が変わると接続が壊れる
  - 妻面(前後の蓋)の**巻き順は前後共通**。頂点zを既に反転しているので、
    巻き順まで反転すると二重反転で法線が内側を向く
- **窓・扉は車体表面の外側に置く独立パネル**。頂点カラーで塗らない。
  ロフトの分割数と無関係に輪郭が鋭く出る
- **前面は曲面に沿うパネル**で作る(`buildFacePanelGeo()`)。
  平らな箱を前面に置くと、前頭部が側方へ回り込むぶん外側の端が車体に沈む
- **客用扉は車体の内側を滑らせる**。外板の扉位置に"穴"を開け、
  奥まった戸袋(内壁+見込み)をロフト側に作る。穴を開けずに扉だけ内側へ置くと
  手前の車体シェルに隠れて見えない
- **鏡像の部品は `scale.z=-1` で作らない**。面の巻き順が裏返って法線が内側を向き、
  同じ材質でも陰影が変わる(片側の扉だけ色が違って見えた)。
  **zを反転したうえで巻き順も反転した専用ジオメトリ**を持つ
- **LOD**で近景=高精細/遠景=簡素。走行中の列車だけでなく**留置中の車両も毎フレーム**
  見直す(初期化時に一度だけだと遠景モデルのまま固定される)

### 4-4. 交差鉄道の車両

**本線の車両の色替えで済ませない**。井の頭線・世田谷線にまで京王8000系の前面が
付いてしまった。線区ごとに専用の簡易モデル(`makeXCar()`)を持ち、
諸元(車体長・幅・軌間・色)を表で分ける。

---

## 5. 検証体制(最初に構築させる)

### 5-1. チェックポイント(本プロジェクトの最終形)

```bash
bash tools/verify_all.sh [対象HTML]
```

| # | ツール | 見るもの |
|---|---|---|
| ① | `check_syntax.py` | `node --check` による構文 |
| ② | `harness.js` | スタブ実行。ReferenceError・初期化順序バグ(TDZ)を検出 |
| ③ | `auto_check.py` | 諸元表 ⇄ 実測値・仕様書(**ソースを正規表現で読む**) |
| ④ | `verify_car.js` | 生成された**ジオメトリの頂点を実測**して照合+幾何的整合 |
| ⑤ | `verify_run.js` | 自動運転を時間発展。停止位置・停車時間・走行線・ホームドア・客用扉・保安装置 |
| ⑥ | `verify_line.js` | 桁幅・架線・吊り点・橋脚の回避・線路の重複 |
| ⑦ | `verify_city.js` | 建物/樹木の配置(道路・河川・鉄道・高架下・相互の重なり)・交差鉄道の列車 |
| ⑧ | `verify_light.js` | 影(向き・範囲・テクセル吸着・near/far)・空・環境マップ・テクスチャの色空間 |

- **③と④は両方必要**。③だけだと「表は正しいがコードが表を無視している」状態を、
  ④だけだと「モデルに合わせて表を書き換えた」状態を見逃す
- `stub_three.js`:THREE を**測定可能な実体**(Vector3/Matrix4/InstancedMesh が
  実際に値を保持する)に差し替え、HTMLの `<script>` を `eval` して
  `const`/`let` で宣言された値まで取り出す。これで**ブラウザ無しで頂点を測れる**
- 検証ツールは**対象HTMLのパスを引数で受け取る**設計にしておく。
  故障注入にも横展開にもそのまま使える

### 5-2. 鉄則1:基準値を HTML から読まない

モデルを直せば基準も動く循環になる。基準は検証スクリプト側に、
**資料(仕様書・配線図・実測)から独立に**書く。

さらに一段深い罠:**「モデルが持つ値どうしを比べる」検査も循環**する。
実際に、扉の開く面をモデルの `p.side` と比べていたため、`p.side` 自体が
左右逆でも素通りしていた。→ 検証側で `seatCar` と同じ式から**独立に再計算**して直した。

### 5-3. 見た目も"数値で"決められる場合がある

描画を見られなくても、**シェーダと同じ式を移植すれば数値で決められる**ことがある。
本プロジェクトではトーンマッピングの露出(`tools/tune_exposure.js`)と
空・フォグの色(`tools/tune_sky.js`)をこの方法で決めた。

1. r128 の `ACESFilmicToneMapping` と Lambert の照明式をそのまま JS へ移植
2. 代表的な面(地表・桁・ホーム・車体・帯・建物…)について、
   変更前と変更後の**表示輝度**を計算
3. **変更前に白飛びしている面は対象から外す**(その表示値自体が本来の色ではない。
   合わせにいくと白飛びを再現してしまう)
4. 残りの中間調の輝度差が最小になる露出を解く

この過程で「地表・桁・ホーム・上屋・レールの5面が 255,255,255 に潰れていた」
ことが判明した。**目視では"明るいな"としか分からない不具合が数値で出る**。

### 5-4. 鉄則2:故障注入(毎回やる)

検査を書いたら、**わざと壊して NG が出ることを確認する**まで終わりにしない。

```bash
sed 's/正しい値/誤った値/' target.html > /tmp/inj.html
node tools/verify_xxx.js /tmp/inj.html   # ← NG が出れば検査は効いている
```

この工程で実際に見つかった「効いていない検査」:
- 失敗時メッセージで `null` を `toFixed()` していて、NG を出す前に例外で落ちる
- 「値が null なら未取得」と判定していて、null が続く間ずっと上書きしてしまう
- 許容差が緩すぎて誤りを吸収してしまう
- **「ずれが小さいこと」を見ていて、"そもそも処理していない"場合も通る**
  (影のテクセル吸着を外しても残差0で合格した。**性質そのもの**=格子座標が
  整数であることを確かめるように直した)

### 5-5. 鉄則3:アサートは書き込みの"前"

置換対象が想定回数だけ出現するか検査し、違えば**書き込む前に停止**する。
書込後 assert だと中途半端な状態のファイルができる(事故あり)。

**インデックスによる範囲切り出しで置換しない**。行番号や文字位置で切ると、
狙った範囲の外まで消える(実際に関数5つと定数3つを巻き添えで削除した)。
必ず厳密一致の文字列で置換する。

### 5-6. 登録簿(registry)方式

作ったものを配列に記録しておくと、検証が実寸を読める。

| 登録簿 | 内容 |
|---|---|
| `TRACKS` / `WIRES` | 敷設した線路と架線(id で対応。線路があるのに架線が無いを検出) |
| `PIERS` | 建てた橋脚のs座標(構造物の上に立っていないか) |
| `PLATS` | ホームの範囲・点字ブロック・端の柵 |
| `PSD` | ホームドアの列(駅・方向・開口位置・開く面) |
| `CITY` | 建物・樹木の世界座標と外接円 |

登録簿は**検証のためだけでなくモデル側も引く**。例えば点字ブロックは
`TRACKS` を引いて「線路のある縁」だけに敷く。

---

## 6. 躓きポイント集(実際に起きた事故と対策)

### 6-1. 座標・向き

| 事故 | 原因 | 対策 |
|---|---|---|
| ホームと反対側の扉が開く | 車体ローカル+zの向きを「+zは南」と決め打った。**この路線は s が西へ増える**ので逆 | 姿勢から計算する(`localZSign()`)。推論で決めない |
| 上り編成だけパンタの位置が違う | 編成内の位置を `j` で指定した。上りでは j=0 が東端 | 西端基準の指定は向きで読み替える |
| 留置線の編成で運転台が内側を向く | 留置線は s が編成順と同じ向きに増える(走行列車と逆) | cabF/cabR を入れ替える |
| カーブ・勾配で車体が線路を向かない | 方位角の取得経路のバグ | **車端2点の実位置の弦**で姿勢を決める |
| 列車が画面端で折り重なる | 線形関数が範囲外の距離を端に丸める | 範囲外は非表示+端到達で初期位置へリセット |

### 6-2. ジオメトリ

| 事故 | 原因 | 対策 |
|---|---|---|
| 後面が透ける | 妻面の巻き順を前後で反転した(頂点zは既に反転済みなので二重反転) | 巻き順は前後共通 |
| 床が張られず下から中が透ける | 断面リングを巡回させ忘れ | リングを閉じる。**符号付き体積**で閉じているか検査できる |
| 片側の扉だけ色が違う | `scale.z=-1` で鏡像を作り、巻き順が裏返った | z反転+巻き順反転の専用ジオメトリを持つ |
| 行先表示器が車体に埋もれる | 曲面の前頭部に平らな箱を置いた | 面に沿うパネルで作る |
| 部品が見えない | 車体表面より内側に置いた | 内側に見せたいものは外板に穴を開ける |
| 駅の上屋が下から見て無い | `cbox` に下面を作っていない | 上下+側面2の4面を張る |
| 断面リングの頂点数が場所で変わり接続が壊れる | 肩から上の埋め点を等間隔で置いた | 帯の境界は固定、埋め点は**個数固定**で等分 |
| 車両のLODが遠景のまま固定 | 初期化時に一度しか判定していない | 毎フレーム見直す(留置中の車両も) |
| `g.add(mesh).position.set(..)` で車両全体が動く | `add()` は Group を返す | メッシュを変数で受けてから置く |

### 6-3. 線路・架線・構造物

| 事故 | 原因 | 対策 |
|---|---|---|
| 「不要な線」が二重に見える | 副本線・留置線を分岐の終わりより先まで伸ばした | 全組み合わせを走査して重複を検出する(`verify_line.js`) |
| 架線の吊り点が柱の真下から外れる | 架線と柱で刻み・基準が別々 | 柱位置を一元化し、架線も同じ配列を参照 |
| 架線が実物にない滑らかな曲線になる | 線路と同じ刻みでサンプリングした | **節点は柱の真下だけ**。柱間は直線(ちょう架線だけ弦の上で垂れる) |
| 柱が高架下の駅舎を貫く | 橋脚の回避表に駅舎が入っていない | 高架下の構造物(道路・鉄道・河川・駅舎)を一つの表にまとめる |
| 桁幅が不要に広い | 桁幅を表で持っていた | 「最外軌道+余裕 / ホーム外縁+余裕 / 標準値」の最大で**算出**する |
| 点字ブロックが線路の無い側に敷かれる | 両縁に一律で敷いた | `TRACKS` を引いて「線路のある縁」だけに敷く |
| 笹塚だけ点字ブロックが片側 | 線路を敷く**前**にホームの付帯物を作った(`TRACKS` が空) | 順序を入れ替える |

### 6-4. 街並み

| 事故 | 原因 | 対策 |
|---|---|---|
| 建物が井の頭線の上に建ち、列車がその中を走ってちらつく | 交差鉄道を「s一定の±20mの帯」で避けた。**斜めに走る線は覆えない**(実際はsが約160m動く) | **世界座標の直線からの距離**で判定する |
| 建物どうしがめり込む | 重なり判定が無い | 世界座標の格子ハッシュに外接円を登録して棄却 |
| 建物が道路・川・高架下に建つ | 除外条件が場当たり | 「占有域」を一つの表(`KEEP`)にまとめ、帯/縦帯/矩形/円/直線の5種類で表現 |

### 6-5. 運転・扉

| 事故 | 原因 | 対策 |
|---|---|---|
| 残り距離が60mずれる | 停止位置を駅中心から一律に測った。ホームがずれている駅で狂う | ホームの実範囲から測る |
| 編成が車体半長ぶんはみ出す | `t.x` は先頭車の"中心"なのに先頭位置として与えた | 意味を揃える |
| 上り列車だけホームからはみ出す | 進行方向で反転すべき補正を反転し忘れ | 方向で符号を反転 |
| 扉が半開きのまま残る | 開閉アニメの更新条件に不感帯を作った | 端で値を止め切る |
| 扉が開かない(3回連続で別原因) | ①開口を帯の色で塗っていた ②パネルが車体の内側にあった ③戸袋の穴が無かった | 症状が同じでも原因は毎回違う。**見た目の推測でなく構造を確認する** |
| 待避線側のホームドアが開く | 自列車は本線を走るのに、自動運転用の扉列を掴んでいた | 走行線から選び直す |
| 開かない扉列を掴んで扉が動かない | 1駅・1方向に扉列が複数ある(待避線と本線) | `openable` で絞る |
| 車両が軌道から浮く | 走行位置を重み付き平均で近似した | 振り分け区間では**線路の敷設式そのもの**を返す |
| 交差鉄道の車輪がレールに埋まる | 車両を固定値の高さに置いた | 線路の敷設と同じ定数(レール頭頂面)から決める |
| 交差鉄道の後ろの車両が宙に浮く | 折り返し範囲を線路の全長で決めた | 編成長を差し引く |

### 6-6. 描画・配色

| 事故 | 原因 | 対策 |
|---|---|---|
| いくら配色を変えても見た目が変わらない | 色管理が無く、明るい面が255に張り付いていた | `outputEncoding`/`toneMapping` を先に正しくする |
| 一部の面だけ不自然に明るい | その色だけ sRGB→リニア変換を通していない | 材質・テクスチャ・頂点カラー・インスタンスカラーを**中央の工場**経由にし、素通しをソースで検出する |
| UIラベルの白がくすむ | 画面上のUIにもトーンマッピングが掛かる | ラベルの材質は `toneMapped:false` |
| 世界中が真っ暗になる | 空ドームを影の対象に入れた(裏面の巨大な球が光を遮る) | 空は cast/receive とも false |
| 地平に線が出る | 空とフォグで別の色を書いた | 同じ定数を使う |
| 空の色が思ったより沈む | トーンマッピング後の値を考えずに16進を書いた | 「見せたい色」から逆算する(`tools/tune_sky.js`) |
| 視点を動かすと影が回る | 光源を「注視点+固定オフセット」に置いた | 向きを固定し、位置は注視点+向き×一定距離 |
| 影の縁がちらつく | 影の中心をテクセル格子に載せていない | 光源座標系の基底を作り、格子へ丸める |
| 影が視界の一部にしか出ない | 写す範囲が固定で、引くと足りない | 半径を視点距離に応じて広げる(テクセルの上限で頭打ち) |

### 6-7. 作業手順

| 事故 | 原因 | 対策 |
|---|---|---|
| 配列の後半要素が消えて桁幅が異常 | **行末コメントが配列要素を飲み込んだ** | 配列は1行1要素 |
| 関数5つと定数3つが巻き添えで消えた | 行番号・文字位置で範囲を切り出して置換した | 厳密一致の文字列でのみ置換する |
| harnessで `Image is not defined` | ブラウザ専用APIのスタブ漏れ | harnessにスタブを追加していく運用 |
| `zDown is not defined`(前方参照) | セクション順序を跨いだ参照 | スタブ実行が検出する。定義済みの関数で書き直す |
| モデル切替で文脈消失 | — | チェックポイント方式+「適用済みマーカーをgrepで確認してから未適用分のみ実行」 |
| 表示灯の点灯/消灯が逆 | 状態変数の名前(点灯時間)と意味(扉が開いている時間)がずれた | 変数は実態で命名し、表示の極性は関数で導出する |

---

## 7. 機能チェックリスト(本プロジェクトの最終仕様)

### 表示
- [ ] **色管理**:著作値(sRGB)→リニア→トーンマッピング→sRGB出力。
      three.js r128 の既定は「sRGB値のまま照明計算して素通し」で物理的に誤り。
      入れないと明るい面が白へ張り付き、いくら配色を詰めても変わらなくなる
- [ ] **影**:長い線区は1枚の影マップで覆えないので、**太陽の向きは固定したまま
      写す範囲だけを注視点に追従**させる。半径は視点距離で可変。
      影の中心は光源座標系のテクセル格子に載せる(ちらつき防止)
- [ ] **空**:グラデーションの空ドーム。**フォグの色は空の地平と同じ値**にする
      (別々に書くと地平に線が出る)。空は影の対象から外す
- [ ] **環境マップ**:空のテクセルをレール・ガラス・水面に映す
- [ ] 俯瞰ビュー(初期視点=地図と同じ向き)/駅クリックでズーム
- [ ] 操作系:ドラッグ=移動、Shift・Ctrl+ドラッグ=回転、ホイール/ピンチ=ズーム
- [ ] ラベル類:駅名/踏切名(道路名も踏切トグルに連動)、トグルボタン
- [ ] 縦断図ナビ(クリックでジャンプ)…画面上部、開閉タブ付き
- [ ] スマホ対応:横画面前提、UI縮小、パネルのスライド開閉、
      運転モード中はラベル系ボタンを強制OFF+非活性、pixelRatio上限・LOD距離短縮

### 線路・構造物
- [ ] 実測座標由来の線形エンジン(全駅・曲線区間・フィレット接続)
- [ ] 高架橋(桁半幅は算出)・橋脚(高架下に構造物がある位置は省く)
- [ ] 架線・架線柱(吊り点=柱の真下、柱間は直線)
- [ ] 副本線・留置線・両渡り分岐器
- [ ] ホーム(点字ブロック・端の柵・上屋)
- [ ] 街並み(占有域と相互非重複で棄却してから配置)
- [ ] 交差鉄道(専用の簡易モデル・線区ごとの軌間)

### 列車
- [ ] 諸元表 → ジオメトリの一方向データフロー
- [ ] 車体ロフト・独立した窓パネル・ジオメトリの前面・台車・床下機器・屋根上
- [ ] LOD(近景/遠景)
- [ ] 自動運転:各駅停車(加速P4/上限60km/h/減速B5)を状態機械
      ('run'→'brake'→'dwell')で実装。本数は3本程度で十分
- [ ] 待避線への進路変更(駅別表)・駅別の停車時間
- [ ] ホームドア(1両4箇所×両数。開口は停止位置と扉配置から自動生成)
- [ ] 客用扉(停車中にホームに面した側だけ開く。車体の内側を滑る)

### 運転モード
- [ ] ワンハンドルマスコン(力行4段/中立/制動7段/非常EB)、
      ↓=力行・↑=制動・N・E キー+画面ボタン、段位チップ表示
- [ ] 速度計(120km/h以上赤字)、次駅と停止位置まで距離
- [ ] 加減速の速度特性(力行:120km/hで25%、制動:120km/hで75%→60km/h以下100%)
- [ ] 応答の一次遅れ(**1.0秒**)
- [ ] ドラッグで首振り・ダブルタップで正面復帰
- [ ] **ATC**:前を走る列車との距離から許容速度を決め、超えていればマスコン操作に
      かかわらず B7 で自動減速。600m先=120km/h / 20m先=0km/h、間は制動距離の式と
      同じ√でつなぐ。**現示ダウンは即時・現示アップ(解除含む)は20秒遅れ**
      (遅れるのは表示だけでなく実際の速度制限も同じ)。表示は5km/h刻み
- [ ] **扉扱い**:停止位置の前後1m以内かつ速度が完全に0km/hで25秒だけ扉を開ける。
      開いている間は自列車の面するホームドアが開き、力行(P1〜P4)は受け付けない
- [ ] **戸閉灯**:実物と同じ極性で「**扉が閉まっていれば点灯**・開いていれば消灯」。
      内部状態は「扉が開いている時間」なので、変数名を"点灯時間"にすると意味が
      常に逆になる(実際に一度取り違えた)
- [ ] 停止位置=ホーム先端の3m手前(起点駅など特例は個別指定)

> **ATCの数値設計の注意**:パターン `v=V√((d-近)/(遠-近))` は
> 「一定減速度で止まる曲線」そのものなので、**制動段の減速度がパターンの
> 要求値を上回っていないと追従できない**。本プロジェクトでは
> 保安装置の制動だけ「速度による鈍化」を受けない扱いにして解決した。

---

## 8. 進め方のコツ(人間側)

- **スクリーンショットを惜しまない**。AIのモデリング反復は「スクショ→差分指摘」
  が最速のループ。特に車両は必須
- 「似てない」だけでなく**どこが違うか1点でも言語化**すると収束が数倍速い
  (例:「紺線は赤帯の上」「前面窓はもっと大きい」「パンタが"く"でなく"フ"」)
- **数値で言えるものは数値で**。「もう少し太く」より「20%太く」のほうが
  一発で決まり、しかも検証に落とせる
- AIが「確認しました」と言っても**画像比較を実際に行ったか疑う**。
  「文章ではなく画像/数値で比較して」と明示的に要求する
- 大改修の前に「現状の実装を確認して」と言わせる(anchor不一致の置換事故を防ぐ)
- **指示が間違っていたら早めに撤回してよい**。本プロジェクトでも
  「ATCの表示を10秒遅れに」→「正しくは現示アップ時に20秒遅れ」と2回改訂した。
  実装済みの方式を差し替えるのは、間違ったまま積み上げるより安い
- 定期的に「コード整理・コメント付与・軽量化」の回を挟む

---

## 9. 他線区への横展開

### 9-1. 方針:フォークしない

コピーして書き換えると、§6 の事故を**新線区で全部やり直す**ことになる。
落とし穴リストは30項目近くあり、これがそのままフォークのコストになる。

### 9-2. 分割の目安(本プロジェクトの実測)

| 区分 | 行数 | 線区依存 |
|---|---|---|
| 与件データ(座標) | 56 | **100%** |
| 線形エンジン | 270 | ほぼ0(RAW駆動) |
| シーン基盤 | 117 | 0 |
| 地表・高架橋・軌道 | 325 | 表の中身のみ |
| 駅 | 267 | 意匠のif連鎖 約50行 |
| 街並み | 138 | 表の中身のみ |
| 車両 | 1133 | 諸元表 約130行 / 生成器 約1000行 |
| カメラ・運転・UI | 431 | **0** |

**実質85%がエンジン、15%がデータ**。運転モード(ATC・マスコン・戸閉灯)は
線区依存ゼロ。すでに主要な線区固有値は表に追い出してあるので下地はできている。

### 9-3. 構成

```
rail3d/
├── engine/   線形 / シーン基盤 / 高架橋 / 軌道 / 駅 / 街並み /
│             車両生成器 / 自動運転・ATC / カメラ・UI
├── lines/<線区名>/
│      line.js           … 実測座標・線形の振れ・待避線・工区・道路・交差鉄道
│      station_design.js … 駅ごとの意匠(if連鎖をテーブル化)
│      cars/<形式>.js    … 諸元表のみ
│      expect.js         … 検証の基準値
├── tools/    ← 変更不要(対象HTMLをパスで受け取る設計になっている)
└── build.py  … engine/* + lines/<線区名>/* を1つの <script> へ連結
```

- `build.py` は**ただの連結**。現行が単一スコープなので、順に繋ぐだけで意味は同じ。
  モジュールシステムもバンドラも不要
- 「ダブルクリックで開ける単一HTML」という性質を維持する。
  fetch でJSONを読む方式はローカルでサーバが必要になるので**採らない**

### 9-4. 順序

1. **挙動を1ミリも変えない分割**。分割後のHTMLで7チェックポイント全PASSが条件
2. TEMPLATE.md(このファイル)を実装に合わせて更新
3. **2線区目を実際に作る**。ここで初めて抽象化の当否が分かる

> **2線区目が無いうちに汎用フレームワーク化しないこと**。
> サンプル1件からの一般化は必ず外す。まず物理的に分けるだけに留め、
> エンジンのAPIは薄いままにしておく。

### 9-5. 検証の基準値の置き場

`lines/<線区名>/expect.js` に線区ごとの基準値を置く。鉄則は変わらない:

> **expect.js は諸元表・配線図・実測から書く。テストを通すために書き換えない。**

ここが崩れると検証体制の価値がゼロになる。

### 9-6. 移植できないもの(正直な見積り)

- **車両の前面**。mm仕様書から生成しているので、形式が変わるたびに新しい仕様書が要る。
  ここが毎回いちばん重い
- **扉数・車体長の違い**は扉配置表の差し替えで届くが、**連接車**は台車配置から作り直し
- **工区(施工段階の分割)**は連続立体交差事業に固有。事業でない線区では
  1グループに畳める逃げ道を用意しておく

---

*このテンプレートは、京王線 連続立体交差事業(笹塚〜仙川)3Dビューア制作
セッション(実測座標線形/京王8000系のジオメトリ・モデリング/7段階の自動検証体制/
運転モードのATC・戸閉灯)の実績に基づく。*
